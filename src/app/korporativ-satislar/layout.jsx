
import "../globals.scss";

export const metadata = {
  title: "Korporativ satışlar",
  description: "Korporativ satışlar",
};

export default function RootLayout({ children }) {
  return (
      <main>
        {children}
      </main>
  );
}