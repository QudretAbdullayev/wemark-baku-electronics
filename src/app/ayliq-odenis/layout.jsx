
import "../globals.scss";

export const metadata = {
  title: "Aylıq ödəniş",
  description: "Aylıq ödəniş",
};

export default function RootLayout({ children }) {
  return (
      <main>
        {children}
      </main>
  );
}