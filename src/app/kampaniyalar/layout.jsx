
import "../globals.scss";

export const metadata = {
  title: "Kampaniyalar",
  description: "Kampaniyalar",
};

export default function RootLayout({ children }) {
  return (
      <main>
        {children}
      </main>
  );
}