
import "../globals.scss";

export const metadata = {
  title: "Mağazalar",
  description: "Mağazalar",
};

export default function RootLayout({ children }) {
  return (
      <main>
        {children}
      </main>
  );
}